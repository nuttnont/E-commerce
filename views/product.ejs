<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>รายละเอียดสินค้า</title>
        <link rel="stylesheet" href="css/all.css" integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf" crossorigin="anonymous" />
    </head>
    <body>
        <%- include('navbar') %>
        <% if(product) {%>
            <div class="container">
                <div class="row">
                    <div class="col-lg-2"></div>
                    <div class="col-lg-8">
                        <div class="card mt-4">
                             <img class="card-img-top " src="<%= product.image %>"  width="300px" height="500px" alt="">
                            <div class="card-body">
                                <h3 class="card-title"><%= product.name %></h3>
                                <h4><%= product.price %></h4>
                                <p class="card-text"><%= product.description %></p>
                                <!-- <a href="" class="btn btn-success">เพิ่มลงตะกร้า</a> -->
                            </div>
                            <div class="card-footer">
                                <form action="/products/cart/" method="post">
                                  <input type="hidden" name="product_id" value="<%= product._id %>">
                                  <input type="submit" name="" value="เพิ่มลงตะกร้า" class="btn btn-success">
                                </form>
                              </div> 
                        </div>
                    </div>
                </div>
            </div>
        <% } %>
    </body>
</html>

